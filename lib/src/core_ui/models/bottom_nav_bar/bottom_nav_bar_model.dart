class ShadowContainerModel {
  final String icon;
  double size;
  double offset;
  bool isVisible;

  ShadowContainerModel(
      {required this.icon,
        required this.size,
        required this.offset,
        required this.isVisible});
}